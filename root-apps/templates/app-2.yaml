apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: root-app-2
  namespace: default
spec:
  project: default
  source:
    repoURL: https://github.com/acainelli/nuke-argocd.git
    targetRevision: HEAD
    path: apps/hello-world-app-2
    helm:
      releaseName: root-app-2
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
